app-id: com.kimi.Console
runtime: org.freedesktop.Platform
runtime-version: "24.08"
sdk: org.freedesktop.Sdk
command: kimi-console
finish-args:
  - --share=network
  - --share=ipc
  - --socket=x11
  - --socket=wayland
  - --socket=pulseaudio
  - --device=all
  - --device=dri
  - --talk-name=org.freedesktop.Flatpak
add-extensions:
  org.freedesktop.Platform.ffmpeg-full:
    directory: lib/ffmpeg
    version: "24.08"
    add-ld-path: "."
    no-autodownload: false
    autodelete: false
modules:
  - name: kimi-console
    buildsystem: simple
    build-commands:
      - cp -r bundle /app/kimi-console
      - install -Dm755 run.sh /app/bin/kimi-console
      - install -Dm644 com.kimi.Console.desktop /app/share/applications/com.kimi.Console.desktop
      - install -Dm644 com.kimi.Console.svg /app/share/icons/hicolor/scalable/apps/com.kimi.Console.svg
    sources:
      - type: dir
        path: ../electron/dist/linux-unpacked
        dest: bundle
      - type: file
        path: com.kimi.Console.desktop
      - type: file
        path: com.kimi.Console.svg
      - type: file
        path: run.sh
