# SMPF v2 — requirements.txt — 2026-08-24 (added portal dependencies)
fastapi>=0.110
uvicorn[standard]>=0.27
requests>=2.31
python-dotenv>=1.0
requests-oauthlib>=1.3

# Portal: registration, login, dashboard. All pure Python — no native
# compilation, no admin rights needed to install any of these.
sqlalchemy>=2.0
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.1

# Only needed for app/connectors/admin_only/gettr_admin.py — Gettr has no
# public API, so this is browser automation, admin's own account only,
# never exposed to clients. Skip installing this if you're not using it.
playwright>=1.40
